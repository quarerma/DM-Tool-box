NODE_ENV=development
PORT=3000
FRONTEND_URL=http://localhost:5173
JWT_SECRET=change-me-in-production
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/dm_tool_box
# Optional: POST endpoint for sending the 2FA new-device verification email.
# Left blank, the service logs the intended email and the code (dev-friendly).
EMAIL_WEBHOOK_URL=
